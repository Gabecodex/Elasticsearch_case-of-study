This code connects to a local Elasticsearch cluster running on localhost at port 9200,
indexes a document into the "my_index" index, and then searches for that document using
a simple match query. The result of the search is then printed to the console.